import { Request, Response } from "express";
import asyncHandler from "../../utils/async.handler";

export const getSetting = asyncHandler(
  async (req: Request, res: Response) => {}
);

export const editSetting = asyncHandler(
  async (req: Request, res: Response) => {}
);
